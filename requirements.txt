# ---------------------------------------------------------------------------
# dr_domain_generalization -- Python dependencies
#
# TORCH IS DELIBERATELY NOT PINNED HERE.
# The RTX 5060 Laptop GPU is Blackwell (compute capability sm_120) and REQUIRES
# a CUDA 12.8+ build of PyTorch. A default `pip install torch` pulls a CPU or
# cu12.4 wheel that will fail at runtime with
#   "no kernel image is available for execution on the device".
#
# Install torch FIRST, from the CUDA 12.8 index (see README section
# "Environment setup"):
#
#   pip install torch==2.9.1 torchvision==0.24.1 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# then `pip install -r requirements.txt` for everything else.
#
# VERIFIED WORKING on the target machine (RTX 5060 Laptop, sm_120, Windows 11,
# Python 3.14.3), 2026-08-19:
#   torch 2.9.1+cu128, torchvision 0.24.1+cu128, timm 1.0.28,
#   albumentations 2.0.8, numpy 2.5.2, pandas 3.0.5, scikit-learn 1.9.0,
#   scipy 1.18.0, matplotlib 3.11.1, pillow 12.3.0, torchmetrics 1.9.0
# The CUDA smoke test and a full training run both pass on this combination.
# ---------------------------------------------------------------------------

# --- core scientific stack -------------------------------------------------
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5

# --- imaging ---------------------------------------------------------------
pillow>=10.3
opencv-python-headless>=4.10
# API verified against 2.0.8 (A.Affine translate_percent/scale, A.Compose)
albumentations>=2.0

# --- models ----------------------------------------------------------------
timm>=1.0.9              # DenseNet121 / ConvNeXt-Tiny / DINOv2 ViT-S/14

# --- metrics ---------------------------------------------------------------
torchmetrics>=1.4

# --- figures ---------------------------------------------------------------
matplotlib>=3.9

# --- embeddings / dimensionality reduction ---------------------------------
# NOTE: only needed for the Phase-4 embedding figures.
umap-learn>=0.5.6

# --- config, logging, progress ---------------------------------------------
pyyaml>=6.0
tqdm>=4.66
tensorboard>=2.17

# --- tests -----------------------------------------------------------------
pytest>=8.2

# NOTE: `seaborn` is intentionally absent -- all figures are plain matplotlib.
# NOTE: `grad-cam` is deferred to the optional explainability phase (Sec. 41).
